import React from 'react';
import ReactDOM from 'react-dom/client';

function App() {
    return (
        <div>
            <h1>Hello, React with Laravel 11!</h1>
        </div>
    );
}

ReactDOM.createRoot(document.getElementById('app')).render(<App />);